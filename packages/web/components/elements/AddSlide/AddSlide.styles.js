import styled from "styled-components";

export const AddSlide = styled.div`
  width: 100%;
  height: 100vh;
  display: grid;
  place-content: center;
  background-color: var(--uefa-grey);

  p {
    border: 1px dashed var(--uefa-green);
    background: var(--white);
    font-size: 1.4rem;
    padding: 0.8rem 1.6rem;
    display: grid;
    place-content: center;
  }
`;
